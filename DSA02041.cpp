#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;

    while (t--)
    {
        long long n;
        cin >> n;

        queue<pair<long long, long long>> q;
        q.push({n, 0});

        while (!q.empty())
        {
            auto [fr, st] = q.front();
                }
    }
}